/** Backend Nestr : Worker Cloudflare de production (mobile parle toujours au prod). */
export const API_URL = "https://nestr-api.thibaut-monier.workers.dev";

/** Deep-link de retour OAuth (doit matcher le `scheme` de app.json). */
export const AUTH_REDIRECT = "nestr://auth";
